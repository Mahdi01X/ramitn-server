services:
  - type: web
    name: ramitn-server
    runtime: node
    region: frankfurt
    plan: free
    rootDir: simple-server
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
